<div class="navbar-wagon">
  <!-- Logo -->
  <div class="navbar-wagon-brand">
  <%= link_to root_path, class: "navbar-wagon-brand navbar-wagon-item navbar-wagon-link" do %>
    <%= image_tag "logonavbar2.png" %>
    <p>L'annuaire des professionnels de la toiture</p>
  <% end %>

<!--     <div class="hidden-xs hidden-sm">
      <div class="navbar-wagon-item">
        <#%= link_to 'Retour', :back, class: "navbar-wagon-item navbar-wagon-link" %>
      </div>
    </div> -->
  </div>

  <!-- Right Navigation -->
  <div class="navbar-wagon-right hidden-xs hidden-sm">

  <%= link_to t(".top_call_to_action", default: "Annuaire"), users_path, class: "navbar-wagon-item navbar-wagon-button btn" %>


<!-- lien guide -->
  <div class="navbar-wagon-item">
    <%= link_to "Guides", guides_path, class: "navbar-wagon-item navbar-wagon-link" %>
  </div>

<!-- lien magazine -->

  <div class="navbar-wagon-item">
    <%= link_to "Magazine", articles_path, class: "navbar-wagon-item navbar-wagon-link" %>
  </div>

    <!-- Different navigation if login or not -->
    <% if user_signed_in? %>
      <%# if user.pro? %>
        <div class="navbar-wagon-item">
          <div class="dropdown">
          <% if current_user.photo_company_logo != nil %>
            <%= cl_image_tag current_user.photo_company_logo.path, width: 50, height: 50, crop: :fill, class: "worker-avatar dropdown-toggle", id: "navbar-wagon-menu", "data-toggle" => "dropdown", style: "border-radius:50%;" %>
          <% else %>
            <%= image_tag "http://placehold.it/30x30", class: "avatar dropdown-toggle", id: "navbar-wagon-menu", "data-toggle" => "dropdown" %>
            <% end %>
            <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
              <li>
                <%= link_to edit_user_registration_path do %>
                  <i class="fa fa-user"></i> <%= t(".profile", default: "Mon profil") %>
                <% end %>
              </li>
              <li>
                <%= link_to workers_path do %>
                  <i class="fa fa-users"></i> <%= t(".profile", default: "Index des candidats") %>
                <% end %>
              </li>
              <li>
                <%= link_to destroy_user_session_path, method: :delete do %>
                  <i class="fa fa-sign-out"></i>  <%= t(".sign_out", default: "Me déconnecter") %>
                <% end %>
              </li>
            </ul>
          </div>
        </div>


      <!-- Profile picture and dropdown -->

      <%# elsif user.worker? %>

<!--       <div class="navbar-wagon-item">
        <div class="dropdown">
        <#% if current_worker.photo != nil %>
          <#%= cl_image_tag current_worker.photo.path, width: 50, height: 50, crop: :thumb, gravity: :face, class: "worker-avatar dropdown-toggle", id: "navbar-wagon-menu", "data-toggle" => "dropdown", style: "border-radius:50%;" %>
        <#% else %>
          <#%= image_tag "http://placehold.it/30x30", class: "avatar dropdown-toggle", id: "navbar-wagon-menu", "data-toggle" => "dropdown" %>
          <#% end %>
          <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
            <li>
              <#%= link_to edit_worker_path(current_worker) do %>
                <i class="fa fa-user"></i> <#%= t(".profile", default: "Mon profil") %>
              <#% end %>
            </li>
            <li>
              <#%= link_to workers_path do %>
                <i class="fa fa-users"></i> <#%= t(".profile", default: "Index des candidats") %>
              <#% end %>
            </li>
            <li>
              <#%= link_to destroy_worker_session_path, method: :delete do %>
                <i class="fa fa-sign-out"></i>  <#%= t(".sign_out", default: "Me déconnecter") %>
              <#% end %>
            </li>
          </ul>
        </div>
      </div> -->
      <%# end %>
    <% else %>
      <!-- Login text link -->
      <%= link_to 'testparams', new_user_session_path(status: :pro) %>
        <button type="button" class="navbar-wagon-item navbar-wagon-link connect-btn-navbar" id="pro-btn-navbar" data-toggle="modal" data-target="#signin_modal" data-id="0">Professionnel</button>
        <!-- Modal telephone -->
        <div class="modal fade" id="signin_modal" role="dialog">
          <div class="modal-dialog">
            <!-- Modal content-->
            <div class="modal-content">
              <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h3>Connexion professionnel</h3>
              </div>
              <div class="modal-body">
              <%= render "devise/sessions/new_navbar" %>
              </div>
            </div>
          </div>
        </div>

        <button type="button" class="navbar-wagon-item navbar-wagon-link connect-btn-navbar" id="worker-btn-navbar" data-toggle="modal" data-target="#signin_modal_worker">Candidat</button>
        <!-- Modal telephone -->
        <div class="modal fade" id="signin_modal_worker" role="dialog">
          <div class="modal-dialog">
            <!-- Modal content-->
            <div class="modal-content">
              <div class="modal-header">
                <button type="button" class="close" data-dismiss="modal">&times;</button>
                <h3>Connexion candidat</h3>
              </div>
              <div class="modal-body">
              <%= render "devise/sessions/new_navbar" %>
              </div>
            </div>
          </div>
        </div>

    <% end %>
  </div>

  <!-- Dropdown list appearing on mobile only -->
  <div class="navbar-wagon-item hidden-md hidden-lg">
    <div class="dropdown">
      <i class="fa fa-bars dropdown-toggle" data-toggle="dropdown"></i>
      <ul class="dropdown-menu dropdown-menu-right navbar-wagon-dropdown-menu">
        <li><a href="#">Some mobile link</a></li>
        <li><a href="#">Other one</a></li>
        <li><a href="#">Other one</a></li>
      </ul>
    </div>
  </div>
</div>

