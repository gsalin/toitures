
<div class="container">
  <div class="row">

    <% if current_user.pro? %>

      <div class="col-xs-12">
        <h1 class="text-center", style="margin-bottom: 40px;">Créer une nouvelle annonce pour mon entreprise</h1>
      </div>

      <div class="col-xs-12 col-md-6">
        <%= simple_form_for(@job_ad) do |f| %>

          <%= f.input :title, label: "Titre de votre annonce", hint: "entre 15 et 50 charactères :", placeholder: "exemple : couvreur-zingueur, ornemaniste, charpentier" %>
          <div class="text-right counter-style">
            <span>charactères :</span>
            <strong><span id="title-job-ad-count"></strong></span>
          </div>
          <%= f.input :diploma %>
          <%= f.input :experience %>
          <%= f.input :remuneration %>

          <%= f.input :description, as: :text, hint: "entre 100 et 500 charactères :", placeholder: "Décrivez ce vous faites, les types de travaux que vous recherchez, vos expériences..."   %>
          <div class="text-right counter-style">
            <span>charactères :</span>
            <strong><span id="description-job-ad-count"></strong></span>
          </div>
          <div class="col-xs-12 col-md-3 col-md-offset-1">
            <%= f.input :profile_photo, as: :attachinary, hint: "Une photo de profil" %>
          </div>
          <div class="col-xs-12 col-md-3 col-md-offset-1">
            <%= f.input :cv, as: :attachinary, hint: "Télécharger votre cv" %>
          </div>

          <div id="position_btn_project" class="text-center">
            <%= f.button :submit, "Publier mon annonce", class: "navbar-wagon-button" %>
          </div>
        <% end %>
      </div>

    <% else %>
      <div class="col-xs-12">
        <h1 class="text-center", style="margin-bottom: 40px;">Editer mon annonce emploi </h1>
      </div>

      <!-- <div class="col-xs-12 col-md-6">
        <#%= simple_form_for(@user) do |f| %>
          <h5>Mes informations personnelles</h5>
          <#%= f.input :first_name, autofocus: true %>
          <#%= f.input :last_name %>
          <#%= f.input :address %>
          <#%= f.input :city, required: true %>
          <#%= f.input :zip_code, required: true %>
          <#%= f.input :office_phone, required: true %>
          <#%= f.input :email, required: true %>
          <#%= f.hidden_field :company, :value => "Candidat" %>
          <!--  <#%= f.input :company, label: "statut", hint: "freelance, diplomé, en recherche d'emploi", placeholder: "Jeune diplômé", required: true %> -->

         <!--  <div id="position_btn_project" class="text-center">
            <#%= f.button :submit, "Mettre à jour mes informations", class: "navbar-wagon-button" %>
          </div>
        <#% end %>
      </div> -->

      <div class="col-xs-12 col-md-6">
         <h5>Mon annonce</h5>
        <%= simple_form_for(@job_ad) do |f| %>
          <%= f.input :title, label: "Titre de votre annonce", hint: "entre 15 et 50 charactères :", placeholder: "exemple : couvreur-zingueur, ornemaniste, charpentier" %>
          <div class="text-right counter-style">
            <span>charactères :</span>
            <strong><span id="title-job-ad-count"></strong></span>
          </div>
          <%= f.input :diploma %>
          <%= f.input :experience %>
          <%= f.input :remuneration %>

          <%= f.input :description, as: :text, hint: "entre 100 et 500 charactères :", placeholder: "Décrivez ce vous faites, les types de travaux que vous recherchez, vos expériences..."   %>
          <div class="text-right counter-style">
            <span>charactères :</span>
            <strong><span id="description-job-ad-count"></strong></span>
          </div>
          <div class="col-xs-12 col-md-3 col-md-offset-1">
            <%= f.input :profile_photo, as: :attachinary, hint: "Une photo de profil" %>
          </div>
          <div class="col-xs-12 col-md-3 col-md-offset-1">
            <%= f.input :cv, as: :attachinary, hint: "Télécharger votre cv" %>
          </div>

          <div id="position_btn_project" class="text-center">
            <%= f.button :submit, "Mettre à jour mon annonce", class: "navbar-wagon-button" %>
          </div>
        <% end %>
      </div>
    <% end %>
  </div>
</div>



