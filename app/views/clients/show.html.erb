<%= render "shared/form", client: @client %>

<% if @available_users != [] %>
  <h3 class="text-center"> Il y a <%= @available_users.count %> prestataires qui près de chez vous</h3>
<% else %>
  <h3 class="text-center"> Il y a <%= @radius_users.count %> prestataires dans votre secteur, affinez votre recherche ci-dessus</h3>
<% end %>

<div class="container">
  <% if @available_users != [] %>
    <% @available_users.each do |user| %>
      <div class="col-xs-12">
      <%= render "shared/card_users", user: user %>
      </div>
    <% end %>
  <% else %>
    <% @radius_users.each do |user| %>
      <div class="col-xs-12">
      <%= render "shared/card_users", user: user %>
      </div>
    <% end %>
  <% end %>
</div>



